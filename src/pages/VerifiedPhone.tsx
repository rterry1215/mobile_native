import React from "react";
import {
    View,
    Text,
    Image,
    StyleSheet,
    ScrollView,
    TouchableOpacity,
    ImageBackground,
} from "react-native";
import Verified from "../components/Signin/Verified";
const Loading = () => {
    return (
        <View>
            <Verified />
        </View>
    )
}

export default Loading;