import React from "react";
import { View } from "react-native";
import Setting from "./components/Setting";

const Profile = () => {
  // Ensure that userInfo is defined before accessing its properties

  return (
    <View style={{}}>
      <Setting />
    </View>
  );
};
export default Profile;
